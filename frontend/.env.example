NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_RESTAURANT_WHATSAPP=233XXXXXXXXX
NEXT_PUBLIC_RESTAURANT_NAME=HallMark Cafe

# Google Cloud key with Places API (New), Maps JavaScript API and Geocoding API
# enabled. Powers checkout address autocomplete, "Use my location" and the pin
# confirmation map. Leave blank to fall back to a plain typed address field.
# Restrict the key by HTTP referrer — it is exposed to the browser.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
